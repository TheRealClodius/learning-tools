# Simplified requirements for Railway deployment
# Use this if main requirements.txt fails due to faiss-cpu/sentence-transformers build issues

# Core web framework dependencies
fastapi>=0.116.1
uvicorn[standard]>=0.35.0
gunicorn>=23.0.0
aiohttp>=3.12.15
httpx>=0.28.1
pydantic>=2.11.7
python-dotenv>=1.1.1
pyyaml>=6.0.2

# API clients
anthropic>=0.60.0
openai>=1.98.0
google-generativeai>=0.8.5

# Slack integration
slack-bolt>=1.23.0

# MCP protocol
mcp>=1.0.0

# Build tools
setuptools>=65.0.0
wheel

# NOTE: If using this file, comment out or remove any imports 
# related to faiss-cpu, sentence-transformers, or memoryos-pro
# from the codebase, or handle them with try/except blocks.